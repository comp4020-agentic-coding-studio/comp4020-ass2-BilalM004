# Assessments page — `src/pages/assessments/`, `src/content/assessments/*.md`

## Needs to satisfy

- Weights sum to exactly 100 (`assessment-weights.test.ts`).
- An `assessments/*crit-participation` entry whose `meta.weeks` covers 1–12
  (`schedule-coverage.test.ts`).

## Plan

- **Crit Participation** — weekly, weighted mode, small weight.
- 2–3 Crossing write-ups / portfolio checkpoints at natural points in the
  semester.
- One capstone-ish piece late in semester, replacing `final-project.md`'s
  placeholder.

## Current state

Only placeholders: `assignment-1.md` (weight 40), `final-project.md`
(weight 60) — sum to 100 coincidentally, content is generic.

## Open questions

- Exact weight split across the above pieces.
- Weighted vs. holistic marking mode per piece.
